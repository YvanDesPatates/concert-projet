<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="{{ path('concertsAVenir') }}">A venir</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('artists') }}">Artists</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_concert_hall_index') }}">Salles</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('concerts') }}">Concerts <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('concert_par_annee', {year: '2023'}) }}">Concerts par année <span class="sr-only">(current)</span></a>
                        </li>
                    </ul>
                </div>
                {% if app.user %}
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            {% if not app.user.firstname is empty %}<a class="nav-link" href="#"> bonjour {{ app.user.firstName }} : </a>
                            {% else %} <a class="nav-link" href="#"> bonjour {{ app.user.email }} : </a> {% endif %}
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_user') }}"> mon compte </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_sign_out') }}"> déconnexion </a>
                        </li>
                    </ul>
                {% else %}
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('app_sign_in') }}"> connexion </a>
                        </li>
                    </ul>
                {% endif %}
            </nav>
        </header>
        <main>
            {% block body %}{% endblock %}
        </main>
        <footer>
            <div id="adresse">
                <img id="here" src="/Symphony/concertProject/assets/here.png" alt="adresse : ">
                <h6> 48 rue du festoiement musical, 75 632 MusicCity - France </h6>
            </div>
        </footer>
    </body>
</html>
{% block style %}{% endblock %}
<style>
    #adresse{
        display: flex;
        width: 100%;
        justify-content: center;
        align-items: flex-end;
        margin-top: 2%;
        padding-top: 1%;
        border-top: solid;
    }
    #here{
        max-width: 30px;
        margin-right: 1%;
    }

</style>

